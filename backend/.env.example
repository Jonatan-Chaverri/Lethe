PORT=4000
NODE_ENV=development

DATABASE_URL=
DIRECT_URL=

STARKNET_RPC_URL=
STARKNET_CHAIN_ID=SN_SEPOLIA

# Access and refresh JWT secrets must be two different values (required).
# Optional: AUTH_JWT_SECRET can be used as fallback for one of them, but the other must be set explicitly.
AUTH_ACCESS_JWT_SECRET=
AUTH_REFRESH_JWT_SECRET=
AUTH_ACCESS_TOKEN_TTL=15m
AUTH_REFRESH_TOKEN_TTL=30d
AUTH_SIGNIN_MESSAGE=Lethe login

# Comma-separated list of allowed frontend origins for CORS.
# Example: https://lethe-sable.vercel.app,https://lethe-production.up.railway.app
CORS_ALLOWED_ORIGINS=
